#' * see to-do list on GitHub
#' 
#' * get_prior_info(): use stanmodel-regex to extract parameter bounds, add them to prior.info
#' and print with T[,] notation to make it clear what, eg, sigma_R ~ normal() means
#' 
#' * par_defs(): can finally make this work!! use stanmodel-regex to extract parameter bounds 
#' and definitions (from comments) for a specific model
#' 

